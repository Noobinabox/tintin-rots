#ALIAS {python}
{
	#run {python}{python -B};
	#debug all on;
targets = [];

def add_target(target):;
	targets.append(target);

def get_targets():;
	print(targets);

#gts;
}

#ALIAS {example}
{
	#python add_target 'John';
	#python get_targets;
}
